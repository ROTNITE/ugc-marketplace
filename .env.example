# Copy this file to .env and adjust values if needed.

# Database (PostgreSQL)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ugc_marketplace
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ugc_marketplace?schema=public
# If you change POSTGRES_* values, update DATABASE_URL accordingly and run `npm run db:reset`.
# Optional (WSL): override host if localhost doesn't reach Docker
# POSTGRES_HOST=127.0.0.1

# NextAuth (Auth.js)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=please_change_me_to_random_string

OUTBOX_CONSUMER_SECRET=change_me

# Payments (basis points, 1500 = 15%)
# COMMISSION_BPS=1500
# Used as fallback if platform settings are not configured in /admin/settings.

# Telegram binding (optional overrides)
# TELEGRAM_BINDING_PEPPER=change_me
# TELEGRAM_BINDING_SECRET=change_me

# Optional: show Prisma query logs
# PRISMA_LOG_LEVEL="info"
# PRISMA_QUERY_LOG=1
