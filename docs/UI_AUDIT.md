# UI Audit

## Область и метод
- Основа аудита: просмотр `src/app`, `src/components`, `docs`, `src/lib/auth.ts`, `prisma/schema.prisma`.
- Ручной проход по UI через браузер/скриншоты: не выполнен в этом окружении (нужен локальный запуск с демо‑аккаунтами).

## Публичные страницы
- `/` — лендинг для гостя (Hero/How it works/Features/Safety/CTA), для авторизованных — роль‑ориентированные home‑экраны (CreatorHome/BrandHome/AdminHome) с карточками быстрых действий и счетчиками.
- `/login`, `/register` — карточки входа/регистрации (Card + формы с Alert/inline‑ошибками, выбор роли, подсказки).
- `/jobs` — лента заказов с левым блоком фильтров (JobFilters), карточками JobCard, алертами ошибок БД и пустого списка; у креаторов показывается JobAlertCreator.
- `/jobs/[id]` — детальная карточка заказа: бейджи платформы/ниши/прав/статуса, секции описания, блок бренда, форма отклика (JobApplyForm) и состояния (не залогинен, не креатор, профиль неполный, уже откликнулся), инструменты бренда (редактировать/дублировать/пауза/ресабмит). Есть блоки Alert для модерации/ошибок.
- `/creators` — каталог креаторов с фильтрами (CreatorFilters), карточками профилей, бейджом VERIFIED, рейтингом и портфолио.
- `/creators/[id]` — публичный профиль креатора: основная инфа, условия, портфолио, отзывы; у бренда — диалог приглашения креатора.
- `/brands/[id]` — публичный профиль бренда: информация, сайт, описание, рейтинг/отзывы, alert про коммуникацию через заказы.

## Кабинет Brand
- `/dashboard` — карточки навигации: создание заказа, сделки, сообщения, профиль, отзывы; бейдж роли и кнопка выхода.
- `/dashboard/jobs` — центр управления заказами с табами‑счетчиками по статусам, списком карточек и быстрыми действиями (редактировать/дублировать/пауза/отклики/приемка). Используются EmptyState/Alert для пустых вкладок.
- `/dashboard/jobs/new`, `/dashboard/jobs/[id]/edit` — JobCreateWizard (многошаговый степпер) с формами и summary‑экраном.
- `/dashboard/jobs/[id]/applications` — список откликов по заказу, карточки креаторов, действия принять/отклонить, переход в чат.
- `/dashboard/jobs/[id]/review` — приемка: эскроу, список сдач, действия approve/request changes, отмена сделки, споры.
- `/dashboard/deals` — вкладки: модерация, отклики, в работе, приемка, завершено; карточки с CTA и бейджами статусов.
- `/dashboard/inbox` и `/dashboard/inbox/[id]` — список диалогов и чат; выделение непрочитанных, бейджи ролей, кнопки открыть/удалить.
- `/dashboard/profile` — форма профиля бренда + блок Telegram‑биндинга.
- `/dashboard/reviews` — оставить отзыв по завершенным заказам и список отзывов о бренде.
- `/dashboard/notifications` — список уведомлений с фильтрами по категориям, кнопки mark‑all/clear.

## Кабинет Creator
- `/dashboard` — карточки навигации: лента заказов, сделки, сообщения, профиль, баланс, отзывы.
- `/dashboard/deals` — вкладки: приглашения, отклики, в работе, на проверке/правки, завершено; CTA на чат/работу/отзыв.
- `/dashboard/invitations` — список приглашений, блоки с сообщением бренда и действия accept/decline.
- `/dashboard/applications` — мои отклики, статусы, ссылки на заказы/чат, кнопка отзыва заявки.
- `/dashboard/work` и `/dashboard/work/[id]` — активные работы и деталь работы: сдача материалов, история версий, эскроу, споры.
- `/dashboard/balance` — баланс, форма запроса выплаты, список ledger‑операций и payout‑заявок.
- `/dashboard/alerts` — сохранённые алерты на заказы, включение/выключение.
- `/dashboard/profile` — форма профиля креатора + верификация + Telegram‑биндинг.
- `/dashboard/reviews`, `/dashboard/notifications`, `/dashboard/inbox` — общие для обеих ролей.

## Админ‑панель
- `/admin` — обзор ключевых очередей (модерация заказов, верификация, выплаты, споры, outbox, корректировки), карточки SectionCard/Stat.
- `/admin/jobs` — модерация заказов с фильтром по статусам, карточки с действиями approve/reject.
- `/admin/creators` — верификация креаторов, карточки с действиями verify/reject.
- `/admin/disputes` и `/admin/disputes/[id]` — список споров и деталь с сообщениями/решениями.
- `/admin/payouts` — заявки на выплаты, approve/reject.
- `/admin/finance` — движение по кошелькам/эскроу, ручные корректировки.
- `/admin/events` — outbox события.
- `/admin/settings` — настройки комиссии/валюты.

## UI‑компоненты (src/components/ui)
- `alert`, `badge`, `button`, `card`, `container`, `empty-state`, `input`, `page-header`, `section-card`, `select`, `stat`, `stepper`, `textarea`.
- Общее: Button variants (primary/secondary/outline/ghost/destructive), Card с header/content/footer, Alert с 4 вариантами.

## Повторяющиеся блоки вне ui
- Навигация/шапка: `SiteHeader` (роль‑ориентированная навигация и счетчики), `SiteFooter`.
- Домашние экраны: `home/*` (карточки с быстрыми действиями и метрики).
- Лендинг‑секции: `landing/*`.
- Заказы: `JobCard`, `JobFilters`, `JobApplyForm`, `JobActions`, `JobResubmitButton`.
- Сделки/чат: `MessageComposer`, `InvitationActions`, `Dispute*`.
- Профили: `creator/profile-form`, `brand/profile-form`, `telegram-binding-card`.
- Платежи: `escrow/escrow-fund-button`, `payouts/*`.

## Проблемы консистентности (для редизайна)
- Статусы отображаются по‑разному: где‑то raw значения (`PAUSED`, `PENDING`), где‑то локализованные лейблы; `PAUSED` в одних экранах = «В работе», в других = «На паузе».
- Смешение языков: “Brand/Creator/Admin”, “Inbox”, “VERIFIED”, “Request changes/Approve”, “ACTIVE/OFF” рядом с русским UI.
- Контрольные символы `` в заголовках/линках (например « К сделкам/к каталогу/к заказам») — видимы в UI.
- Непоследовательные пустые состояния: часть страниц использует `Alert`, часть — `EmptyState`, часть — простые текстовые блоки.
- Непоследовательные контейнеры: где‑то `Container` + `PageHeader`, где‑то ручные `mx-auto max-w-*` блоки.
- Разные версии карточек/статблоков: есть `Stat`/`SectionCard`, но метрики на home‑страницах и в других местах сделаны вручную.
- Разные паттерны CTA: где‑то кнопки, где‑то текстовые ссылки в одной и той же роли/сценарии.
- В `SubmissionForm` кнопка удаления элемента обозначена “?” — неясное действие.
- В формах нет явной унификации подсказок/ошибок (inline‑ошибки vs Alert вверху).

## Пустые, ошибочные и процессные состояния
- Ошибки доступа: почти везде Alert (“Нужен вход”, “Недоступно”).
- Пустые списки: встречаются Alert/EmptyState (jobs, deals, inbox, payouts, reviews, alerts, notifications).
- Процессные состояния: локальные “loading” тексты на кнопках (например “Отправляем...”), но нет единого loader/skeleton.
- Статусы процессов: бейджи показывают moderation/job/application/submission/escrow/payout/dispute состояния, но нет единого справочника отображения.

## Что требуется для ручного прогона
- Локальный запуск с демо‑аккаунтами Brand/Creator (из seed) и проход по всем экранам для проверки верстки, текстов, spacing и визуальных артефактов.
- Скриншоты ключевых экранов: лендинг, jobs list/detail, creator/brand public profiles, dashboard (jobs/deals/inbox/work/balance/profile/notifications), admin (jobs/creators/disputes/payouts/settings/finance/events).
